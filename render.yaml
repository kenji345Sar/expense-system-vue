services:
  - type: web
    name: expense-system
    env: docker
    dockerfilePath: ./docker/prod/Dockerfile
    plan: free
    envVars:
      - key: APP_ENV
        value: production
      - key: APP_DEBUG
        value: false
      - key: APP_KEY
        generateValue: true
